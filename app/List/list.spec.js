'use strict';

describe('userMang.list module', function() {

  beforeEach(module('userMang.list'));

  describe('list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listCtrl = $controller('listCtrl');
      expect(listCtrl).toBeDefined();
    }));

  });
});